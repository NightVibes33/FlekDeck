name: Upstream LiveContainer Signing/JIT-Less Parity (manual legacy)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy Duy parity workflow disabled; VibeContainers parity is authoritative.'
