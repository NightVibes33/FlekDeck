name: VibeContainers Signing/JIT-Less Parity (legacy)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Replaced by vibecontainers-core-parity-v2.yml'
