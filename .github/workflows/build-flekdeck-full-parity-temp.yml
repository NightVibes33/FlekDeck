name: FlekDeck Full Parity Temp IPA

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  canonical-sidestore-build:
    uses: ./.github/workflows/build-flekdeck-sidestore-parity-temp.yml
    secrets: inherit
